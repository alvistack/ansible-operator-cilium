apiVersion: alvistack/v1alpha1
kind: Cilium
metadata:
  labels:
    app.kubernetes.io/name: ansible-operator-cilium
    app.kubernetes.io/managed-by: kustomize
  name: cilium-sample
spec:
  # TODO(user): Add fields here
